study:
  title: "mTSP heuristic comparison"
  objective: "Compare solution quality under a fixed time budget"
  metric: "MINSUM"
  time_limit_seconds: 60

baselines:
  - rand+nn
  - 2opt+greed

advanced:
  - grasp
  - lkh-wrapper-v2

factors:
  node_counts: [50, 100, 200]
  salesman_counts: [2, 3, 5]

notes:
  - "Use the same instances for all compared methods."
  - "Separate tuning runs from final evaluation runs."
